<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car ffeine - 자동차 모델</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .model-list {
            display: flex;
            flex-wrap: wrap;
            gap: 16px; /* 간격 조절 */
            justify-content: center;
        }
        .model-item {
            flex: 1 0 21%; /* 각 항목이 전체의 21%를 차지 (4개씩 배치) */
            max-width: 21%;
            box-sizing: border-box;
        }
        .model-btn {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            background: none;
            border: none;
            cursor: pointer;
        }
        .model-img {
            width: 100%;
            height: 150px; /* 이미지 높이 고정 */
            object-fit: cover; /* 이미지 크기 비율 유지 및 크기에 맞게 자름 */
        }
        .model-item span {
            margin-top: 8px; /* 텍스트와 이미지 사이 간격 */
        }
    </style>
</head>
<body>
    <header class="header-top">
        <div class="logo">
            <a href="/" class="title-a"><h1>Car ffeine</h1></a> 
        </div>
        <div class="head-rt">
            <div class="button-container">
                <% if (user) { %>
                    <a class="btn btn-outline-success btn-login"><%= user.nickname %></a>
                    <a href="/logout" class="btn btn-outline-success btn-signup">로그아웃</a>
                <% } else { %>
                    <a href="/login" class="btn btn-outline-success btn-login">로그인</a>
                    <a href="/signup" class="btn btn-outline-success btn-signup">회원가입</a>
                <% } %>
            </div>
        </div>
    </header>

    <nav class="zzul">
        <button type="button" class="btn btn-outline-red" onclick="location.href='/chat'">그룹채팅</button>
        <button type="button" class="btn btn-outline-dark" onclick="location.href='/carboard'">자동차 게시판</button>
        <button type="button" class="btn btn-outline-primary" onclick="location.href='/freeboard'">자유게시판</button>
        <button type="button" class="btn btn-outline-secondary" onclick="location.href='/newsboard'">뉴스게시판</button>
        <button type="button" class="btn btn-outline-success" onclick="location.href='/termboard'">용어게시판</button>
    </nav>

    <main class="main">
        <!-- 커뮤니티 베스트 섹션 시작 -->
        <div class="community-best">
            <!-- 페이지 제목 -->
            <h1 class="board-main">원하는 모델을 선택하세요!</h1>
            
            <!-- 모델 목록을 담는 리스트 -->
            <ul class="model-list">
                <!-- 모델이 존재하는 경우 -->
                <% if (models && models.length > 0) { %>
                    <% models.forEach(model => { %>
                    <li class="model-item">
                        <!-- 모델 버튼, 클릭 시 해당 모델의 상세 페이지로 이동 -->
                        <button class="model-btn" onclick="location.href='/carDetail/<%= model.id %>'">
                            <!-- 모델 이미지 표시 -->
                            <img class="model-img" src="<%= model.image_url %>" alt="<%= model.name %>">
                            <!-- 모델 이름 표시 -->
                            <span><%= model.name %></span>
                        </button>
                    </li>
                    <% }) %>
                <!-- 모델이 존재하지 않는 경우 -->
                <% } else { %>
                    <p>선택한 브랜드에 대한 모델이 없습니다.</p>
                <% } %>
            </ul>
        </div>
    </main>
    
    <footer>
        <p>&copy; 2024 자동차 커뮤니티. Carffeine.</p>
    </footer>
</body>
</html>
